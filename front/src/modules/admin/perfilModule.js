import Typography from '@material-ui/core/Typography';

function PerfilModule(props) {
    return (
        <div>
            <Typography variant="h4" gutterBottom>PerfilModule</Typography>
        </div>
    );
}

export default PerfilModule;